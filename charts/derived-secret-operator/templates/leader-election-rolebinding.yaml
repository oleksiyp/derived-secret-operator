{{- if .Values.leaderElection.enabled -}}
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "derived-secret-operator.fullname" . }}-leader-election-rolebinding
  labels:
    {{- include "derived-secret-operator.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "derived-secret-operator.fullname" . }}-leader-election-role
subjects:
- kind: ServiceAccount
  name: {{ include "derived-secret-operator.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
{{- end }}
