apiVersion: v1
kind: Namespace
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: derived-secret-operator
    control-plane: controller-manager
  name: derived-secret-operator-system
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: derivedsecrets.secrets.oleksiyp.dev
spec:
  group: secrets.oleksiyp.dev
  names:
    kind: DerivedSecret
    listKind: DerivedSecretList
    plural: derivedsecrets
    singular: derivedsecret
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    - jsonPath: .status.secretName
      name: Secret
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DerivedSecret is the Schema for the derivedsecrets API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of DerivedSecret
            properties:
              annotations:
                additionalProperties:
                  type: string
                description: Annotations to apply to the generated secret
                type: object
              keys:
                additionalProperties:
                  description: DerivedKeySpec defines how to derive a single key
                  properties:
                    length:
                      description: Length is the length of the generated secret (only
                        for custom type)
                      maximum: 256
                      minimum: 22
                      type: integer
                    masterPassword:
                      default: default
                      description: MasterPassword is the name of the MasterPassword
                        to use
                      type: string
                    type:
                      description: Type is the type of secret to generate
                      enum:
                      - password
                      - encryption-key
                      - custom
                      type: string
                  required:
                  - type
                  type: object
                description: Keys is a map of key names to their derivation specifications
                minProperties: 1
                type: object
              labels:
                additionalProperties:
                  type: string
                description: Labels to apply to the generated secret
                type: object
              type:
                default: Opaque
                description: Type is the type of secret to create
                type: string
            required:
            - keys
            type: object
          status:
            description: status defines the observed state of DerivedSecret
            properties:
              conditions:
                description: Conditions represent the current state of the DerivedSecret
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastUpdated:
                description: LastUpdated is the last time the secret was updated
                format: date-time
                type: string
              ready:
                description: Ready indicates whether the derived secret is ready
                type: boolean
              secretName:
                description: SecretName is the name of the generated secret
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: masterpasswords.secrets.oleksiyp.dev
spec:
  group: secrets.oleksiyp.dev
  names:
    kind: MasterPassword
    listKind: MasterPasswordList
    plural: masterpasswords
    singular: masterpassword
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    - jsonPath: .status.dependentSecrets
      name: Dependent Secrets
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: MasterPassword is the Schema for the masterpasswords API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of MasterPassword
            properties:
              annotations:
                additionalProperties:
                  type: string
                description: Annotations to apply to the generated secret
                type: object
              length:
                default: 86
                description: Length is the length of the generated master password
                maximum: 256
                minimum: 22
                type: integer
              secret:
                description: |-
                  Secret defines the secret where the master password is stored
                  If not specified, defaults to <name>-mp in the operator namespace
                properties:
                  create:
                    default: true
                    description: Create indicates whether to create the secret if
                      it doesn't exist
                    type: boolean
                  name:
                    description: Name is the name of the secret
                    type: string
                required:
                - name
                type: object
            type: object
          status:
            description: status defines the observed state of MasterPassword
            properties:
              conditions:
                description: Conditions represent the current state of the MasterPassword
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              dependentSecrets:
                description: DependentSecrets is the count of DerivedSecret resources
                  using this MasterPassword
                type: integer
              ready:
                description: Ready indicates whether the master password secret is
                  ready
                type: boolean
              secretName:
                description: SecretName is the name of the secret containing the master
                  password
                type: string
              secretNamespace:
                description: SecretNamespace is the namespace of the secret containing
                  the master password
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: derived-secret-operator
  name: derived-secret-operator-controller-manager
  namespace: derived-secret-operator-system
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: derived-secret-operator
  name: derived-secret-operator-leader-election-role
  namespace: derived-secret-operator-system
rules:
- apiGroups:
  - ""
  resources:
  - configmaps
  verbs:
  - get
  - list
  - watch
  - create
  - update
  - patch
  - delete
- apiGroups:
  - coordination.k8s.io
  resources:
  - leases
  verbs:
  - get
  - list
  - watch
  - create
  - update
  - patch
  - delete
- apiGroups:
  - ""
  resources:
  - events
  verbs:
  - create
  - patch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: derived-secret-operator
  name: derived-secret-operator-derivedsecret-admin-role
rules:
- apiGroups:
  - secrets.oleksiyp.dev
  resources:
  - derivedsecrets
  verbs:
  - '*'
- apiGroups:
  - secrets.oleksiyp.dev
  resources:
  - derivedsecrets/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: derived-secret-operator
  name: derived-secret-operator-derivedsecret-editor-role
rules:
- apiGroups:
  - secrets.oleksiyp.dev
  resources:
  - derivedsecrets
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - secrets.oleksiyp.dev
  resources:
  - derivedsecrets/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: derived-secret-operator
  name: derived-secret-operator-derivedsecret-viewer-role
rules:
- apiGroups:
  - secrets.oleksiyp.dev
  resources:
  - derivedsecrets
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - secrets.oleksiyp.dev
  resources:
  - derivedsecrets/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: derived-secret-operator-manager-role
rules:
- apiGroups:
  - ""
  resources:
  - secrets
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - secrets.oleksiyp.dev
  resources:
  - derivedsecrets
  - masterpasswords
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - secrets.oleksiyp.dev
  resources:
  - derivedsecrets/status
  - masterpasswords/status
  verbs:
  - get
  - patch
  - update
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: derived-secret-operator
  name: derived-secret-operator-masterpassword-admin-role
rules:
- apiGroups:
  - secrets.oleksiyp.dev
  resources:
  - masterpasswords
  verbs:
  - '*'
- apiGroups:
  - secrets.oleksiyp.dev
  resources:
  - masterpasswords/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: derived-secret-operator
  name: derived-secret-operator-masterpassword-editor-role
rules:
- apiGroups:
  - secrets.oleksiyp.dev
  resources:
  - masterpasswords
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - secrets.oleksiyp.dev
  resources:
  - masterpasswords/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: derived-secret-operator
  name: derived-secret-operator-masterpassword-viewer-role
rules:
- apiGroups:
  - secrets.oleksiyp.dev
  resources:
  - masterpasswords
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - secrets.oleksiyp.dev
  resources:
  - masterpasswords/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: derived-secret-operator-metrics-auth-role
rules:
- apiGroups:
  - authentication.k8s.io
  resources:
  - tokenreviews
  verbs:
  - create
- apiGroups:
  - authorization.k8s.io
  resources:
  - subjectaccessreviews
  verbs:
  - create
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: derived-secret-operator-metrics-reader
rules:
- nonResourceURLs:
  - /metrics
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: derived-secret-operator
  name: derived-secret-operator-leader-election-rolebinding
  namespace: derived-secret-operator-system
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: derived-secret-operator-leader-election-role
subjects:
- kind: ServiceAccount
  name: derived-secret-operator-controller-manager
  namespace: derived-secret-operator-system
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: derived-secret-operator
  name: derived-secret-operator-manager-rolebinding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: derived-secret-operator-manager-role
subjects:
- kind: ServiceAccount
  name: derived-secret-operator-controller-manager
  namespace: derived-secret-operator-system
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: derived-secret-operator-metrics-auth-rolebinding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: derived-secret-operator-metrics-auth-role
subjects:
- kind: ServiceAccount
  name: derived-secret-operator-controller-manager
  namespace: derived-secret-operator-system
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: derived-secret-operator
    control-plane: controller-manager
  name: derived-secret-operator-controller-manager-metrics-service
  namespace: derived-secret-operator-system
spec:
  ports:
  - name: https
    port: 8443
    protocol: TCP
    targetPort: 8443
  selector:
    app.kubernetes.io/name: derived-secret-operator
    control-plane: controller-manager
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: derived-secret-operator
    control-plane: controller-manager
  name: derived-secret-operator-controller-manager
  namespace: derived-secret-operator-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: derived-secret-operator
      control-plane: controller-manager
  template:
    metadata:
      annotations:
        kubectl.kubernetes.io/default-container: manager
      labels:
        app.kubernetes.io/name: derived-secret-operator
        control-plane: controller-manager
    spec:
      containers:
      - args:
        - --metrics-bind-address=:8443
        - --leader-elect
        - --health-probe-bind-address=:8081
        command:
        - /manager
        image: ghcr.io/oleksiyp/derived-secret-operator:latest
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8081
          initialDelaySeconds: 15
          periodSeconds: 20
        name: manager
        ports: []
        readinessProbe:
          httpGet:
            path: /readyz
            port: 8081
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          limits:
            cpu: 500m
            memory: 256Mi
          requests:
            cpu: 10m
            memory: 128Mi
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          readOnlyRootFilesystem: true
        volumeMounts: []
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      serviceAccountName: derived-secret-operator-controller-manager
      terminationGracePeriodSeconds: 10
      volumes: []
---
apiVersion: secrets.oleksiyp.dev/v1alpha1
kind: MasterPassword
metadata:
  name: default
spec:
  length: 86
